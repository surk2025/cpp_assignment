#include<iostream>
#include<string>
using namespace std;

int main(){
    string s = "surjeet";
    // s.substr(idx,len);
    // cout<<s.substr(2,3);
    int n = s.length();
    // s.substr(idx,len);
    cout<<s.substr(n/2);

}